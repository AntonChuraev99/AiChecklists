{
  "name": "gisti-mcp-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Remote MCP server exposing Gisti checklists (read + edit) over OAuth. Cloudflare Worker.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "agents": "^0.17.3",
    "hono": "^4.6.14",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260708.0",
    "tsx": "^4.23.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.6",
    "wrangler": "^4.0.0"
  }
}
