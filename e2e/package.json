{
  "name": "gisti-web-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright e2e tests for Gisti AI Checklists wasmJs web build",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0"
  }
}
