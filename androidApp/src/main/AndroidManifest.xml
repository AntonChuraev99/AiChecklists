<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Alarm-style per-item reminders (over the lock screen). RESTRICTED since API 34: auto-granted
         only for CATEGORY_ALARM (this app's declared core function); gated at runtime by
         NotificationManager.canUseFullScreenIntent() and degrades to a heads-up otherwise. -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Required for Google Ads attribution on Android 13+ (API 33) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <application
        android:name="com.antonchuraev.homesearchchecklist.GistiAndroidApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules_legacy"
        android:dataExtractionRules="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name="com.antonchuraev.homesearchchecklist.MainActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              App Links for the SEO-gallery deep-link. Claims the whole app.gisti-ai.com host
              (App Links can't match a query string); the app inspects g=create&template={slug} in
              MainActivity code. SEO gallery pages live on the DIFFERENT host gisti-ai.com (NOT
              claimed) so they keep opening in the browser. autoVerify uses the assetlinks.json
              served at https://app.gisti-ai.com/.well-known/assetlinks.json.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="app.gisti-ai.com" />
            </intent-filter>
        </activity>

        <!--
          ACTION_PROCESS_TEXT trampoline + 4 selection-toolbar entries.
          ProcessTextActivity itself is not exported; the 4 aliases are the exported entry
          points (one menu item each, with its own label). The alias class name tells the
          trampoline which action was tapped (see ProcessTextContract).
        -->
        <activity
            android:name="com.antonchuraev.homesearchchecklist.ProcessTextActivity"
            android:exported="false"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.NoDisplay" />

        <activity-alias
            android:name="com.antonchuraev.homesearchchecklist.ProcessTextCreateAiAlias"
            android:targetActivity="com.antonchuraev.homesearchchecklist.ProcessTextActivity"
            android:exported="true"
            android:label="@string/process_text_create_ai">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name="com.antonchuraev.homesearchchecklist.ProcessTextAddToExistingAlias"
            android:targetActivity="com.antonchuraev.homesearchchecklist.ProcessTextActivity"
            android:exported="true"
            android:label="@string/process_text_add_to_existing">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name="com.antonchuraev.homesearchchecklist.ProcessTextNewChecklistAlias"
            android:targetActivity="com.antonchuraev.homesearchchecklist.ProcessTextActivity"
            android:exported="true"
            android:label="@string/process_text_new_checklist">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name="com.antonchuraev.homesearchchecklist.ProcessTextFillAiAlias"
            android:targetActivity="com.antonchuraev.homesearchchecklist.ProcessTextActivity"
            android:exported="true"
            android:label="@string/process_text_fill_ai">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity-alias>

        <!--
          Drop androidx.work.WorkManagerInitializer from the androidx.startup provider.

          WorkManager's own manifest registers that initializer here, so WorkManager was built on
          EVERY process start inside ContentProvider.onCreate() — before Application.onCreate(),
          where a throw is fatal and uncatchable by the app. On 2026-08-13 a Pixel 8 Pro on 1.19.1
          could not launch at all: its ROM reports API 34 but ships a framework.jar without
          JobScheduler.forNamespace, so androidx.work's (correctly SDK_INT-guarded) call died with
          NoSuchMethodError inside installProvider.

          No app code enqueues work, so every start paid this for nothing. Glance does reach
          WorkManager (SessionManagerImpl.startSession -> enqueueUniqueWork) when the home-screen
          widget updates, so WorkManager still gets built — now lazily, on first widget update,
          via Configuration.Provider on GistiAndroidApplication, with the same default
          Configuration the initializer used.

          The <provider> itself must stay: five other initializers (emoji2, ProcessLifecycle,
          ProfileInstaller, RevenueCat, OkHttp) are declared under it.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- FCM service for re-engagement push (class lives in :composeApp androidMain). -->
        <service
            android:name="com.antonchuraev.homesearchchecklist.push.GistiFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Default channel for notification-payload pushes (data-only campaign uses
             "promotions" explicitly; this only covers any future notification-payload sends). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="promotions" />

        <!-- Emits push_dismissed when a re-engagement notification is swiped away. Wired as the
             notification's delete intent (class lives in :composeApp androidMain push package). -->
        <receiver
            android:name="com.antonchuraev.homesearchchecklist.push.PushDismissReceiver"
            android:exported="false" />

        <!-- Reminder Notification Receiver (handles checklist-level and per-item reminders) -->
        <receiver
            android:name="com.antonchuraev.homesearchchecklist.notification.ReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.antonchuraev.aichecklists.ACTION_REMINDER_FIRE" />
                <action android:name="com.antonchuraev.aichecklists.ACTION_REPEAT_FIRE" />
                <action android:name="com.antonchuraev.aichecklists.ACTION_ITEM_REMINDER_FIRE" />
                <action android:name="com.antonchuraev.aichecklists.ACTION_ITEM_REPEAT_FIRE" />
            </intent-filter>
        </receiver>

        <!-- Alarm-style full-screen reminder shown over the lock screen for per-item reminders
             that opted into reminderFullScreen. Launched only via setFullScreenIntent from
             ReminderReceiver; not exported, excluded from recents. -->
        <activity
            android:name="com.antonchuraev.homesearchchecklist.notification.FullScreenReminderActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />

        <!-- Boot Completed Receiver: reschedule reminders after device reboot -->
        <receiver
            android:name="com.antonchuraev.homesearchchecklist.notification.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Exact Alarm Permission Receiver: reschedule as exact when permission granted -->
        <receiver
            android:name="com.antonchuraev.homesearchchecklist.notification.ExactAlarmPermissionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Local retention auto-pushes (streak-save / overdue / weekly digest). Triggered only by
             explicit-component AlarmManager PendingIntents from RetentionPushScheduler, so no
             intent-filter is needed (the action is read from the explicit intent). exported=false. -->
        <receiver
            android:name="com.antonchuraev.homesearchchecklist.retention.RetentionPushReceiver"
            android:exported="false" />

        <!-- Checklist Widget -->
        <receiver
            android:name="com.antonchuraev.homesearchchecklist.widget.ChecklistWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/checklist_widget_info" />
        </receiver>

        <activity
            android:name="com.antonchuraev.homesearchchecklist.widget.config.WidgetConfigActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Invisible activity that toggles a checklist item from the widget. Used via
             actionStartActivity to avoid the Glance LazyColumn list-adapter trampoline
             crash (Crashlytics babbf348). Transparent + no history/recents so the user
             only ever sees the widget update. -->
        <activity
            android:name="com.antonchuraev.homesearchchecklist.widget.ToggleItemActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
    </application>

</manifest>
